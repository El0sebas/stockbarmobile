{
  "version": 2,
  "installCommand": "git clone --depth 1 --branch 3.47.2 https://github.com/flutter/flutter.git .flutter && .flutter/bin/flutter config --enable-web && .flutter/bin/flutter pub get",
  "buildCommand": ".flutter/bin/flutter build web --release --base-href /",
  "outputDirectory": "build/web"
}
